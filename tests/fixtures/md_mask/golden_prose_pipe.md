# Prose pipes without separator — must NOT be treated as tables
This is prose with a | pipe and another | pipe.

Another line with | pipes | but no separator row after it.

And a line with escaped \| pipe inside prose.

No tables here, just pipes in sentences.
