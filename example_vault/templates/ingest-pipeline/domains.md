# Domains

**The single copy of the domain and subdomain map.** Produced by
[`01-assess/A1`](01-assess/QUESTIONS.md) and [`A2`](01-assess/QUESTIONS.md), refined by
[`05-domain-model/A7`](05-domain-model/QUESTIONS.md); read by every other step —
filtering, translation, classification, and success criteria all scope their answers
against it.

> **Do not restate this map inside a step file.** A second copy drifts the moment A7's
> completeness loop adds a domain, and then two steps are scoped to different maps. Link
> here instead.

Where there are few broad domains and many subdomains, the subdomain is the level most
work actually happens at — so both levels live here. Everything else about the domain
model stays with the questions that produce it: out-of-scope domains and why in
`01-assess/A3`, authorities in `A4`, contested splits and their rulings in `A5`,
hard/soft dependencies in `05-domain-model/A6`.

| Domain | Subdomain | One-line description | Found via | In scope? |
|--------|-----------|---------------------|-----------|-----------|
| | | | | |

Leave the subdomain cell blank on a row that describes the domain itself.

---

**Last updated:**
**By:**
**A7 completeness iteration:**
